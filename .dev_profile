#!/usr/bin/env zsh

#Set DEVPATH PATH
DEVPATH=$PWD

# Set PATH so it includes user's private bin if it exists
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi

# Set Aliases
if [ -f $DEVPATH/.aliases ]; then
     . $DEVPATH/.aliases
fi

# Load functions
if [ -f $DEVPATH/.functions ]; then
     . $DEVPATH/.functions
fi